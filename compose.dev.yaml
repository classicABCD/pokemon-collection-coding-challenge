# Local development only: exposes PostgreSQL for a backend started with ./mvnw spring-boot:run
# Usage: docker compose -f docker-compose.yml -f compose.dev.yaml up -d postgres
services:
  postgres:
    ports:
      - "127.0.0.1:5432:5432"
